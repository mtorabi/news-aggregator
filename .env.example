# Copy this file to .env and fill with your API keys
# Example values are empty — replace with real keys before running the app

REACT_APP_NEWS_API_KEY=
REACT_APP_NY_TIMES_API_KEY=
REACT_APP_GUARDIAN_API_KEY=

# Notes:
# - REACT_APP_NEWS_API_KEY is used for the NewsAPI.org source (named newsAPI in code).
# - REACT_APP_NY_TIMES_API_KEY is used for the New York Times API (named nyTimes in code).
# - REACT_APP_GUARDIAN_API_KEY is used for The Guardian API (named theGuardian in code).
# After filling, run your app (for example: npm start) or set these in your CI environment.
